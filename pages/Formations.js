import React from "react";
import FormationContainer from "../components/Formation/Formations";

const Formations = () => {
  return (
    <div>
      <FormationContainer />
    </div>
  );
};

export default Formations;
